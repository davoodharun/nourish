(function() {
  var services;

  services = angular.module('nourish.services', []);

  services.factory('Stores', function($http, $log, Stores) {
    var stores
    stores = 'test'

    return stores
  });

  

}).call(this);
